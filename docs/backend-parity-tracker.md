# Backend Parity & Hardening Tracker

Tracks the suboptimal/quirky items found in the PHP backend (`gpt/backend`) and their status in
each backend. The TypeScript backend (`gpt/backend_typescript`) is a faithful mirror, so most items
are either **fixed in both** or **mirrored as-is** (deliberately replicating PHP). The Python backend
(`gpt/backend_python`) is now a third implementation covering every route PHP serves (Phases 1–8
complete 2026-09-07 — see `backend_python/README.md`'s Status table), so most rows below carry a PY
column alongside PHP/TS on the same fixed/mirrored basis. DB-schema changes are written as `.sql` for
the owner to run (migrations are applied manually).

**Legend:** ✅ done · ⬜ to do · ➖ N/A · 🪞 mirror as-is (no fix intended) · 🚫 won't do (decided) · 🧪 differential-tested vs live PHP

**Deployment context / threat model:** internal corporate access, behind authentication (not public
internet). This lowers the priority of anti-abuse hardening (e.g. login rate-limiting) but NOT of
correctness or authentication-integrity items (e.g. #1 token verification still matters).

Last updated: 2026-09-16

## A. Fixes (correct in BOTH backends)

| # | Item | PHP | TS | PY | Tested | Notes |
|---|------|-----|----|----|--------|-------|
| 1 | `firebaseAuth` verifies the Firebase ID token server-side (RS256 vs Google certs, aud/iss bound to projectId; identity from verified claims) | ✅ | ✅ | ✅ | 🧪 | Reject-parity tested (empty→400, garbage/forged/old-attack→401). Happy path needs real social-login check. New dep: reach `googleapis.com` for certs (cached 1h). |
| 2 | JWT secret fails closed (no weak `your-secret-key…` / empty fallback) | ✅ | ✅ | ✅ | 🧪 | PHP: throw if `JWT_SECRET` empty in `AuthController`, `WebAuthnController`, `MiddlewareProcessor`. TS: already fails closed via `required('JWT_SECRET')`. Verified normal boot/auth unaffected. |
| 4 | Public `/api/v1/auth` dispatcher gates protected actions centrally | ✅ | ✅ | ✅ | 🧪 | `handleAction` rejects link/unlink_phone, upgrade_plan, *_app_key with 401 when unauthenticated (defense-in-depth; methods already self-checked, so responses unchanged). Parity confirmed byte-identical. |
| 3 | Login rate-limiting / lockout | 🚫 | 🚫 | 🚫 | — | **Won't do for now (2026-06-30).** Target is internal corporate access behind authentication, so brute-force exposure is low. Revisit if the backend is exposed to the public internet. |
| 7 | Remove runtime DDL from `AuthController` (`ensureUserSubscriptionColumns`) | ✅ | ➖ | ➖ | 🧪 | Columns confirmed present in live DB → method + constructor call deleted; no migration needed. TS never did this. Live auth smoke OK. |
| 7b | Same runtime-DDL pattern in 11 other PHP files (MCP, memory repos, traces, file storage, admin, settings, heal) | ⬜ | ➖ | ⬜ | — | NOT yet ported to TS. Handle per-file when porting: if the table/columns are already in `schema/chatbot.sql` → delete the runtime DDL; if the runtime `CREATE TABLE IF NOT EXISTS` is the ONLY creator → add it to the schema (migration) first, then delete. Do NOT blind-delete. |
| 9 | `AgentController::chat` validation failures (`Agent ID is required`, not found, `Input message is required`, `tools must be an array`) answer HTTP 200 with a corrupted body `{"success":false,...}null` on PHP — the method returns void after `sendJsonResponse`, and `index.php`'s dispatch epilogue then encodes `null` and forces 200 | ⬜ | ✅ | ✅ | 🧪 | TS validates before SSE headers are set and returns real `res.status(...).json(...)` (`AgentController.ts` chat, comment cites the PHP quirk directly). PY returns the correct status + JSON (Phase 5). PHP fix: `return` the array instead of echoing, or `exit` after `sendJsonResponse`. Differential chat-validation case intentionally absent until PHP is fixed. |
| 8 | `prompt_library.user_id` `varchar(255)` → integer to match JWT id | ✅ | ✅ | ✅ | 🧪 | Migration `schema/migrations/2026-06-30_prompt_library_user_id_to_int.sql` **applied** (column now `int NOT NULL`). PHP already bound the int natively (no change). TS: typed `user_id` as `number`, dropped `String()` casts. getTree parity re-confirmed. FK still optional (1 orphan row to resolve first). |
| 5 | Pre-prod hardening: CORS `*`, shared JWT secret, browser-shipped voice keys | ⬜ | ⬜ | ⬜ | — | Already on the separate pre-prod hardening checklist; cross-listed here. |
| 10 | PHP: `AdminController` has NO admin-role gate — every `/api/v1/admin/*` method it serves (users, providers, keys, usage, MCP admin, costs, exchange rates) is reachable by ANY authenticated user | ⬜ | ⬜ | ⬜ | — | SECURITY. Ported verbatim across TS and PY (see `AdminController.ts`'s docblock / `admin_controller.py`) for parity; fix in PHP first (add the role check the other admin controllers carry), then mirror both ports. |
| 11 | PHP: `EVIWebhookController::handleWebhook` calls `ToolsManager::getAvailableTools()`/`executeTool()`, which do not exist → uncaught `\Error` → 500 on every webhook with a valid `tool_name`; it also performs no signature verification | ⬜ | ✅ | 🪞 | 🧪 | TS deliberately did NOT reproduce this (docblock: "Latent PHP bug, deliberately NOT reproduced" — implements the evident intent via `getRegisteredFunctions()`/`execute()`). PY mirrors the bug on purpose (public, unauthenticated endpoint; the undefined methods' intended replacement is ambiguous) — Python's `AttributeError` (an `Exception`) is caught locally, same `{success:false,error,status_code:500}` shape as PHP's outer `Throwable` handler, message text inherently differs. `tests/differential/test_hume_evi.py::test_evi_webhook_valid_tool_name_500_shape_parity` pins shape (not text) parity vs PHP. Owner: wire the real tool manager API (TS's approach is the reference) + add HMAC verification; PY should then follow suit. |
| 12 | PHP: `round()` half-tie (round-half-away-from-zero) differs from Python/JS default rounding on real cost data (e.g. `21790*15/1_000_000`) | ⬜ | ✅ | ✅ | 🧪 | TS: `phpRound`/`phpRound2` in `AdminController.ts`. PY: `_php_round` in `admin_controller.py` (final wave: promote to `phpcompat.php_round` and audit genesis/heal/voice/usage_logger money math). `tests/differential/test_admin_usage_mcp_costs.py` compares costs/usage fields with a rounding comparator (`same_rounded`). |

## B. Mirror as-is (replicate PHP — no fix intended unless re-prioritized)

| # | Item | Status | Notes |
|---|------|--------|-------|
| 6 | `firebaseAuth` writes arbitrary `provider` into an enum column | 🪞 | Both store the request `provider` as PHP does. Revisit only if #8-style schema cleanup happens. |
| 9 | Single inline per-user app key; regen silently invalidates old | 🪞 | Mirrored; matches PHP `users.app_key_*` semantics. |
| 10 | `getTree` loose `==` parent match, drops orphans | 🪞 | TS reproduces the same tree-building (orphans dropped). |
| 11 | Inconsistent messages ("Invalid or expired credential" vs "…token") | 🪞 | Mirrored verbatim. |
| 12 | Duplicate call paths (RESTful routes shadowed by action dispatcher) | 🪞 | Both supported, as in PHP. |
| 13 | Controllers only ever use the contexts DB | 🪞 | TS uses the contexts pool for controllers, same as PHP. |
| 14 | Body parsed unconditionally; invalid JSON → `{}` | 🪞 | TS body parser mirrors `json_decode(...) ?? []`. |
| 15 | register returns 200 (not 201); response timestamps are wall-clock | 🪞 | Mirrored. |
| 16 | `firebaseAuth` synthesizes `<phone>@phone.auth` emails | 🪞 | Mirrored (now from the verified phone claim). |
| 17 | Controller "response" array mixes transport keys with body | 🪞 | TS `handle()` replicates the convention. |
| 18 | `json_encode` full-precision floats + slash/unicode escaping | 🪞 | Cosmetic; JSON parsers (incl. the frontend) normalize. TS emits clean values. |
| 19 | PY: `password_hash` emits `$2b$` (PHP `$2y$`); both verify each other's hashes | 🪞 | Cross-backend login test in `tests/differential/test_auth.py`. |
| 20 | PY: `filter_var(FILTER_VALIDATE_EMAIL)` approximated by RFC-5322-ish regex | 🪞 | Edge cases (quoted local parts, IP literals) may differ; frontend validates first. |
| 21 | PY: `PromptLibraryController::create` returns `data.id` as string (PDO `lastInsertId`) | 🪞 | Mirrored; contexts return int. |
| 22 | PY: `debugAuth.php_version` is a mirrored constant (`8.2.4`) | 🪞 | Update when XAMPP PHP changes. |
| 23 | PY: stored JSON (`context_data`, `packages.capabilities`) written without `\/` escaping | 🪞 | Decodes identically; PHP reads it fine. |
| 24 | PY: WebAuthn assertions are not cryptographically verified (same as PHP) | 🪞 | Pre-existing PHP gap; listed for visibility. |
| 25 | PY: `date('Y-m-d H:i:s')` uses `PHP_TIMEZONE` env (default Europe/Berlin) | 🪞 | Must match php.ini `date.timezone`. |
| 26 | PY: login for a social-only user (`users.password` NULL) returns 401 'Invalid email or password'; PHP throws a TypeError → 500 | 🪞 deliberate deviation | PHP behavior is an uncaught crash, not design; frontend only reads `success:false`. |
| 27 | PY: JWT decode disables PyJWT's `verify_sub` (PyJWT ≥2.10 rejects non-string `sub`); PHP tokens carry an integer `sub` | 🪞 | Required for cross-backend token interop; signature/exp/iat/nbf still verified. |
| 28 | PY: `php_empty()` helper mirrors PHP `empty()` (incl. the string "0") wherever PHP uses `empty()` on request input; `??` is ported as `v if v is not None else default`, never `.get(k, default)` | 🪞 | Applied throughout `AuthController` (login/register/firebaseAuth/linkPhone, fixed 2026-09-05 — those five sites previously used plain truthiness, diverging on `"0"`). Convention for all future ports. |
| 29 | PY: `client_flag FOUND_ROWS` NOT set — live PHP returns 404 on a same-title context rename, so PyMySQL's default affected-rows count already matches | 🪞 | Verified live 2026-09-05. |
| 30 | PY: SSE event payloads are clean JSON (`phpjson.dumps`: no `\/` escaping, unicode unescaped) | 🪞 | Decodes identically in the browser. |
| 31 | PY: `SessionSearchService` connects to the contexts DB lazily on first use (PHP connects in the constructor) | 🪞 | A connection failure surfaces as `{'error': …}` from the tool instead of a constructor throw. |
| 32 | PY: providers other than Claude return the PHP "Provider 'x' not found" error path | 🪞 | Until Phase 2b ports openai/gemini/kimi/grok/deepseek/mistral. |
| 33 | PY: regular (non-streaming) chat runs the memory auto-updater via a hook in `main.py` (`ctx['_after_response']`) after the body is rendered but BEFORE it is written to the client | 🪞 | Same latency as PHP under mod_php (no `fastcgi_finish_request`, ~3s when memory is on); the hook can never fail the response. |
| 34 | PY: `AttachmentDispatcher` import is wrapped in the same `try` as PHP's attachment block | 🪞 | Until Phase 2d ports it, the `ImportError` is swallowed and attachments are ignored. |
| 35 | PY: `_handleVerification` / `_handleComparison` raise `NotImplementedError` until Phase 2d | 🪞 | A verification/compare-enabled request emits a humanized `error` SSE event plus an `error` usage row after the main answer, rather than a PHP-style verification section. |
| 36 | PY: `llm_function_usage_stats` is never created by the Python backend | 🪞 | `UsageTracker.trackFunctionCall` is ported but unused; PHP would `CREATE TABLE` on first call. |
| 37 | PY: `markHeadersInitialized` is declared on `StreamingClientInterface` | 🪞 | PHP declares it only on the concrete `SSEHubClient`. |
| 38 | PY: client-tool name validation is stricter — Python's `$` does not match before a trailing `\n` | 🪞 deliberate deviation | A tool name like `"route_to\n"` is rejected where PHP's `preg_match` would pass it to the provider. |
| 39 | PY: the 2048-byte client-tool description cap slices bytes and decodes with `errors='ignore'` | 🪞 deliberate deviation | PHP `substr` can emit a split multibyte char that `json_encode` then rejects. |
| 40 | PY: streaming path returns `stop_reason` as `None` like PHP's `makeStreamingRequest` | 🪞 | The earlier plan test asserting `'tool_use'` was wrong. |
| 41 | PY: `SseStream.end()` is called at the same point as PHP's `fastcgi_finish_request()` (inside the memory block, after usage logging) | 🪞 | When that block does not run, the stream is closed by `main.py`'s `finally`. |
| 42 | PY: SSE headers (`text/event-stream`) are sent lazily on the first event; PHP sends them eagerly on entry to `handleStreamingChat` | 🪞 | Every 2a exit path emits at least one event, so the wire is identical; a future handler that returns before any `send()` would answer a bare 200 where PHP answers an empty event-stream. |
| 43 | PY: the SSE bridge queue is unbounded (frames buffer in memory for a stalled client); PHP's `flush()` applies socket backpressure | 🪞 | Chat-sized payloads (tens of KB); revisit if an endpoint streams large bodies. |
| 44 | PY: Grok/Kimi/DeepSeek ignore `base_url` from `system_llm_settings` (class constants, like PHP) | 🪞 | Mirrors PHP; the TS port diverged here. |
| 45 | PY: `CustomProvider` request timeout `providers.<name>.timeout` ?? `{'gamma4': 90}` ?? 600 s, connect 15 s, via `httpx.Timeout` | 🪞 | Same values as PHP's Guzzle options. |
| 46 | PY: Gemini `streamChat` = `chat()` + one `onChunk(text)`; no true streaming | 🪞 | Spec §4; identical event skeleton to PHP. |
| 47 | PY: provider HTTP errors map 429/401/other from `httpx.HTTPStatusError`; network errors (`httpx.RequestError`) become `apiError(provider, str(e), 0)` | 🪞 | Category mapping (429 / 401 / other) matches PHP. The message TEXT differs from Guzzle's — httpx's `str(e)` is the status line only, and Guzzle's embeds a body summary — so the ported providers rebuild it (see rows 50 and 55). |
| 48 | PY: `ProviderRequestFactory` interface check is `issubclass(cls, HttpRequestBuilderInterface)` (PHP `class_implements`) | 🪞 | Same fallbacks. |
| 49 | PY: OpenAI streaming is decided by `options['stream'] ?? true`; the `openai.streaming` config key is never read | 🪞 | PHP parity. |
| 50 | PY: ` \| Response: <body>` is appended to the `apiError` message on the paths where PHP's Guzzle message would have carried a body excerpt: OpenAI, DeepSeek, Custom, Gemini, Claude STREAMING (`makeStreamingRequest`), and Kimi's non-JSON fallback | 🪞 | NOT everywhere: Grok extracts `error.message` out of the body instead (PHP mirror), and Claude's NON-streaming `_makeRequest` keeps a bare `str(e)` because PHP only has the idiom on its streaming path. `humanizeProviderError` depends on the body reaching it. |
| 51 | PY: `getDefaultSystemPrompt` is public on every provider (PHP private) | 🪞 | The shared mixin calls it. |
| 52 | PY: DeepSeek `disableThinkingForThisCall` also triggers on a forced `tool_choice` (PHP 166-177) | 🪞 | Mirrored. |
| 53 | PY: Gemini `parseHttpResponse` returns OpenAI-shaped `tool_calls` (`{id, type, function{name, arguments}}`, PHP 1273-1280) | 🪞 | Mirrored. |
| 54 | PY: `php_strval` helper mirrors PHP `strval` for enum coercion in tool schemas (bool → "1"/"", float 1.0 → "1") | 🪞 | Used wherever a tool schema enum value needs PHP-style stringification. |
| 55 | PY: the appended ` \| Response: <body>` is truncated by `guzzle_body_summary` (`app/providers/_http.py`) at 120 chars + `' (truncated...)'`, mirroring `GuzzleHttp\Psr7\Message::bodySummary` | 🪞 | Only the truncation rule is mirrored; Guzzle also returns null for empty/non-seekable bodies and for summaries containing non-printable characters — here an empty body yields `''` and binary bodies pass through. Applied at all six append sites of row 50. |
| 56 | PY: all seven providers share one `ssl.SSLContext` (`app/providers._http.SHARED_SSL_CONTEXT`) via `httpx.Client(verify=...)` | 🪞 | Not a behaviour deviation — curl (and so Guzzle) caches CA material process-wide, while httpx 0.28 builds a fresh context per Client. `_initializeDefaultProvider` constructs every enabled provider per chat request: ~410 ms → ~39 ms. |
| 57 | PY: `humanizeProviderError`'s 🌐 branch also matches `nodename`/`servname`/`name or service not known`/`getaddrinfo` | 🪞 | Behavioural parity over regex-text parity: PHP/curl says "Could not resolve host", httpx surfaces the OS getaddrinfo text. Same humanized line either way. |
| 58 | PY: `fixSchemaForGemini` / `convertToolsToGeminiFormat` are `@classmethod`s recursing through `cls.` | 🪞 | Reproduces PHP's `self::` inside a trait, which resolves to the USING class — so calls entering through the mixin land on `GeminiProvider`'s override. |
| 59 | Differential: the chat path has NO `model` override in the request body (PHP `ChatController::chat` never reads `$input['model']`; only `agent()` does, at ChatController.php:947), so provider error parity cannot be forced per-request | ⏳ | `tests/differential/test_chat.py::test_streaming_error_parity_bad_key` stays skipped: exercising it needs a throwaway `system_llm_settings` row (or user API-key row) pointing at an unroutable host / carrying an invalid key. Error-message parity is covered by the unit matrix (`test_humanize_provider_error_matrix.py`) instead. |
| 60 | Differential: NON-streaming parity is asserted per provider (`tests/differential/test_providers.py`), comparing HTTP status + JSON key order + `usage` shape, ignoring `text` and token counts | 🪞 | Streaming was already covered; spec §6 asks for both. The status is COMPARED, not pinned to 200, so a live provider that is rate limited (openai, observed 429) or whose model host is offline (gamma4, observed 503) yields opportunistic error-path parity: same status, same humanized `error` string on both backends. |
| 61 | PY: `ProviderController` closes every `AIPortfolioAssistant` it constructs (`finally: assistant.close()`) | 🪞 | Python-only; PHP relies on request teardown. Up to eight httpx clients per call otherwise wait for GC. |
| 62 | PY: `AgentDelegationFunctions` is names-only until Phase 5 (`getToolNames()`); the handlers land with `AgentRunner` | 🪞 | `/api/v1/providers` advertises the three names exactly as PHP. |
| 63 | PY: built-in Functions call `raise_for_status()` on FMP responses so 4xx/5xx reach the same `except` PHP's Guzzle exception reaches | 🪞 | Error text differs only after the PHP prefix (`Failed to fetch …: `). |
| 64 | PHP+PY: `MetalsNewsFunctions` deleted (it was commented out in PHP; Metals News is an MCP server) | ✅ | Owner rule 2026-09-06: commented-out functions are deleted in every implementation. |
| 65 | PY: `get_all_transactions` defaults `sort` to `date` when omitted; PHP re-reads the undefined key (`PortfolioFunctions.php:281`) → `ORDER BY t.  DESC` → SQL error → `{"error": …}` | 🪞 deliberate deviation | PHP behavior is an accidental crash, not design (same precedent as row 26). PHP fix is the owner's call. |
| 66 | PY: watchlist alert comparisons (`get_watchlist_with_market_data`) use a PHP-8 loose-comparison helper: numeric strings compare numerically, `NULL` prices compare as `''` (so `NULL <= alert` is true, `NULL >= alert` is false) | 🪞 | DECIMAL columns arrive as strings from PyMySQL; mirrors PHP's `>=`/`<=` on the same values. |
| 67 | PY: `add_to_watchlist` returns `watchlist_id` as an int (`db.insert()`); PHP's `lastInsertId()` returns a string | 🪞 deliberate deviation | Owner-sanctioned (2026-09-06 final review): the plan's `lastInsertId()` → `db.insert(...)` porting rule types the return as an int; not worth widening every insert-id call site to `strval()` for this one JSON field. |
| 68 | PHP+PY: the chat body's `user_id` overrides the authenticated user id for tool execution AND for the attachment ownership check (`ChatController` chat/agent/verify/compare: `$input['user_id'] ?? $request['user_id']` → `AttachmentDispatcher::buildPrefix(..., user_id)`) | 🪞 | Pre-existing PHP behaviour kept for parity. Since Phase 2d an authenticated user can pass another user's id plus that user's `attachment_ids` and have that document inlined — reachable NOW, not only after Phase 5. Owner to fix in PHP first; the port then mirrors. |
| 69 | PY: `ExecutionTraceStore.ensureTable()` performs no DDL (presence check + log); PHP `CREATE TABLE IF NOT EXISTS` on demand | 🪞 | Spec rule "no runtime DDL"; the table exists live. Same for every later `ensure*` site. |
| 70 | PY: `SkillToolBridge` directory is `$SKILL_TOOL_BRIDGE_DIR` or `/tmp/synergy-workflow-tool`; PHP uses `sys_get_temp_dir()` (a `/var/folders/…/T` path on this Mac) | 🪞 | Each backend's runner and bridge live in the same process family, so the dirs need not be shared. |
| 71 | PY: upload MIME detection = byte sniffer + libmagic-like text typing (svg/xml/html/json) + PHP's extension fallback; PHP uses `finfo`. On this box XAMPP's old libmagic types `.docx`/`.pptx` as `application/octet-stream`, so PHP 415s them while Python accepts them via PHP's own OOXML branch | 🪞 deliberate deviation (environment) | Pinned by `tests/differential/test_chat_upload.py`; pdf/xlsx/txt/md/csv/html/json/svg/xml parity verified against the XAMPP php `finfo`. |
| 72 | PY: document text extraction uses pypdf / python-docx / openpyxl / python-pptx (PHP: smalot/pdfparser, PhpWord, PhpSpreadsheet, PhpPresentation) | 🪞 | Block layout, headers, separators, elision text, truncation marker and note strings are verbatim; extracted text may differ; python-docx drops hyperlink-run text; latin-1 `.txt` bytes decode with replacement. |
| 73 | PY: `fetch-url` network failures read `Fetch failed (httpx <ExceptionClass>): <err>` (PHP: `Fetch failed (curl errno N): <err>`) | 🪞 | Same 502 and dict shape. |
| 74 | PY: `fetch-url` does not verify TLS (`verify=False`), mirroring PHP's `CURLOPT_SSL_VERIFYPEER=false` | 🪞 | LAN deployment; parity over hardening. |
| 75 | Differential: `fetch-url`'s private-address 403 cannot be compared — both backends are called from localhost and PHP's `callerIsLoopback()` bypasses the guard | 🪞 | Unit-tested on both the refuse and bypass branches. |
| 76 | PY: `fetch-url` body decoding: unknown charset names fall back to utf-8 with replacement (PHP returns the raw bytes) | 🪞 | Edge case; header/meta charsets handled like PHP. |
| 77 | PY: `ExecutionTraceStore.diagnose` sorts with Python's stable sort; PHP `usort` is stable too since PHP 8.0 (this box: 8.2.4) | 🪞 | No divergence on PHP ≥ 8.0; noted only because PHP 7 would differ. |
| 78 | PY: `SkillToolBridge` result files are read/written as UTF-8 with replacement (PHP: raw bytes) | 🪞 | Malformed bytes yield `None` like PHP's failed `json_decode`. |
| 79 | PY: upload MIME sniffer mirrors libmagic for svg/xml/html/json and for `#!` shebang scripts / C source (`text/x-shellscript`, `text/x-c` → 415 like PHP); other libmagic text subtypes (`text/x-makefile`, `text/x-diff`, …) may still be typed `text/plain` and accepted where PHP's `finfo` refuses | 🪞 | Calibrated against the XAMPP php `finfo`; rows pinned in `test_chat_attachment_controller.py`. |
| 80 | PY: every Phase 3 `ensure*TableExists`/`ensure*ColumnsExist` is a presence check (`SHOW TABLES/COLUMNS … LIKE`, cached) + log; PHP runs `CREATE TABLE IF NOT EXISTS`/`ALTER TABLE` on demand | 🪞 | Spec "no runtime DDL". All tables/columns exist live except `users.universalfs_api_key` (PHP's own ALTER branch is dead, so PHP never creates it either); guarded SELECTs omit missing columns and use PHP's post-ALTER default. |
| 81 | PY: PHP encodes an EMPTY string-keyed array as `[]` — `php_array()` is applied to every dict PHP DECODED as an array that may be empty on the wire; `new stdClass()` defaults stay `{}` | 🪞 | Global rule since Phase 3 (`keys`, `models`, `by_provider`, tool `args`, …). |
| 82 | PY: `trim()` → `php_trim()` (PHP charlist only, `php_strval` coercion: `trim(null)` = `''`, `trim(true)` = `'1'`); never Python `.strip()` | 🪞 | Global rule since Phase 3; `saveKeys` skips null key values like PHP. |
| 83 | PY: `SettingsController::clearKeys` wipes ALL of the user's `user_api_keys` + `user_model_selections` rows (no provider parameter), exactly like PHP | 🪞 | The differential snapshots and restores user 3's rows byte-for-byte. |
| 84 | PY: API keys saved via Python decrypt on PHP and vice versa (AES-256-CBC, `sha256(jwt_secret)` key, random IV, `base64(iv‖ct)`) | ✅ | Proven by the cross-backend save/read/clear differential and a pin against PHP CLI ciphertext. |
| 85 | PY: `FileStorageController` implements the `local` provider fully; non-local providers depend on the PHP-only `universalFS` package and return PHP's "adapter unavailable" path | 🪞 deliberate deviation | Same local root as PHP (`htdocs/storage/…`). |
| 86 | PY: `VoiceController::getEphemeralToken` calls xAI over httpx; the differential runs it only with `DIFF_VOICE_TOKEN=1` (default skip) | 🪞 | Unit-tested with MockTransport. |
| 87 | PY: `MCPProxyController` network failures report the httpx exception message where PHP reports `Connection failed: <curl error> (code: <errno>)` | 🪞 | Same dict shape and status. |
| 88 | PHP+PY: `GET /api/v1/mcp/app` and the legacy `GET /api/mcp-app.php` are PUBLIC routes (`MiddlewareProcessor.php:32-34`) | 🪞 | Mirrored in the Python `PUBLIC_ROUTES`. |
| 89 | PY: `HealController::record` is gate-less in PHP (empty body upserts a zero-value `heal_spend` row) — mirrored; the differential snapshots/restores the row | 🪞 | PHP design, not a port choice. |
| 90 | PY: `GenesisProposerTest.php`'s `buildWorkflowPrompt` cases are stale (method removed in PHP; workflow proposals are deterministic in the controller) — not ported | 🪞 | Documented in `genesis_proposer.py`. |
| 91 | PY: `AppKeyController::destroy` is a soft delete (`revoked_at`) and `index` never filters revoked keys, exactly like PHP | 🪞 | The differential hard-deletes its own test rows by SQL. |
| 92 | PY: `ToolsController::classifyIntent` `args` default is `{}` (PHP `new stdClass()`); a decoded empty `args` object re-encodes as `[]` (PHP `json_decode(..., true)`) | 🪞 | Pinned at the JSON-bytes level. |
| 93 | PHP+PY: `GET /api/v1/storage/providers` always fails — `FileStorageController::getProviders` (PHP:98) calls the undefined method `getUniversalFSClient()` → 500 | 🪞 | Mirrored (Python raises the same message). Owner to fix in PHP first. |
| 94 | PHP+PY: `FileStorageController::listLocalFiles`/`readLocalFile` (PHP 399–479) have NO path-traversal guard — `../` in `path` reads outside the user's folder as the web-server user | 🪞 | Mirrored for parity. SECURITY: fix in PHP first (reject paths escaping the user root), then mirror. |
| 95 | PY: MCP proxy header merge is last-write-wins on exact-case duplicates where PHP sends duplicate header lines | 🪞 | Unreachable in practice (custom headers are auth-only). |
| 96 | PHP+PY: `GET /api/v1/mcp/app` injects `serverUrl` into an inline script unescaped (identical in both) | 🪞 | Latent XSS via a quote in a server URL; fix in PHP first, then mirror. |
| 97 | PHP+PY: `POST /workflow-schemas` 500s — live `workflow_schemas` lacks `strict` (and `updated_at`) while `WorkflowSchemaRepository::create` (PHP 66) writes `strict` unconditionally | 🪞 | Live-DB gap, not a port defect. No DDL from Python. Differential asserts failure parity (same 500 + `'strict'` error). Owner: add the columns via `.sql`, then extend the round-trip test. |
| 98 | PHP+PY: `AgentController::listTools` returns `mcp: []` always — PHP never calls `MCPToolsLoader::loadToolsForUser()` before `getToolDefinitions()` (unlike `ToolsController`) | 🪞 | Verified live (`counts: builtin 21, mcp 0, delegation 3`). Fix in PHP first (load tools like ToolsController), then mirror. |
| 99 | PY: playbook-agent tool auto-derivation (`AgentController::create/update`, PHP 135/266 → `PlaybookAgentTools::forUser`) returns `[]` until the Playbook package lands in Phase 5 | ⏳ | Isolated in `_playbookAgentToolsForUser`; PHP's own never-throws fallback is `[]`. Carry-forward: Phase 5 Task 4 ports `PlaybookAgentTools.php` and wires it in. |
| 100 | PY: `WorkflowOutputStorage::getUniversalFSClient` skips PHP's `file_exists(bootstrap.php)` gate and returns `null` + PHP's "No universalFS API key configured" log | 🪞 | Converges with live PHP (no `universalfs.api_key` configured). Non-local providers answer with PHP's unavailable path (Phase 3 ruling). |
| 101 | PY: `WorkflowGraphRepository::saveGraphOnce` catches `Exception` (Python) where PHP catches `\Exception` only (`\Error` propagates with the transaction open) | 🪞 | Strictly safer (rollback + re-raise); unreachable on well-formed payloads. |
| 102 | PY: `WorkflowGraphRepository::createEdge` raises `KeyError` on a payload missing `from_node_id`/`to_node_id` where PHP warns and coerces to 0 | 🪞 | Internal callers (`saveGraphOnce`) always supply both keys. |
| 103 | PY: descending sorts (`WorkflowOutputStorage::listFromLocalFallback` `usort($b <=> $a)`, repository `display_order` sorts) use Python stable `sorted(reverse=True)` | ✅ | Tie order verified equal (PHP 8 usort is stable). |
| 104 | PHP+PY: `WorkflowController::runEvents` answers HTTP 200 on every branch — `index.php:149` only honours a `result['status_code']` key, which `runEvents`'s three error branches and success branch never set (PHP's own `http_response_code()` calls there are dead code) | 🪞 | Confirmed live (not-found case returns 200 with `{"error":"Run not found"}`). Mirrored — Python's `render()` uses the same `status_code` default. |
| 105 | PY: `WorkflowController::uploadNodeDocument`'s file field is `file`, not `document` (`$_FILES['file']`, `WorkflowController.php:1440,1445`; matches `frontend/assets/js/workflow-editor.js:7239`) | 🪞 | Ported to the real PHP field name. |
| 106 | PHP+PY: `uploadNodeDocument`/`deleteNodeDocument` have no local-storage fallback in PHP — upload 503s ("Storage system (UniversalFS) not available") when the adapter is unavailable; delete logs the remote-delete failure and continues (metadata still removed) | 🪞 | Mirrored; consistent with the Phase 3 universalFS-unavailable ruling (row 100). |
| 107 | PY: `WorkflowController::getLocalStoragePath` ported though PHP never calls it (dead code) | 🪞 | Ported per the task brief's explicit method list; harmless. |
| 108 | PY: `WorkflowRunLog` writes/reads `backend/storage/workflow-runs/{runId}.jsonl` — the same physical directory PHP uses (no `backend_python`-local copy), so a run started by either backend is visible to the other's `runEvents` | ✅ | Verified: 82 existing `*.jsonl` files live in that directory as of 2026-09-07; no `workflow_runs_dir` config override in live PHP config. Python `append` has no `LOCK_EX` equivalent; a run is driven by one backend at a time, so no interleaving is expected. |
| 109 | PY: `ParallelAgentExecutor::dispatchChunk` / `runConcurrentRound` fan out with `ThreadPoolExecutor(max_workers=len(chunk))` instead of `curl_multi`; results are assembled in chunk **submission order**, never completion order (PHP iterates `$curlHandles` in input order after `curl_multi_exec` completes) | ✅ | Ordering proven by the overlap test; `close()` releases the owned httpx client. |
| 110 | PY: server-side waits on the client-tool bridge (`SkillToolBridge.awaitResult`, playbook gates) block the uvicorn worker thread exactly as PHP blocks the Apache request | 🪞 | Memory `skill-workflows-need-browser`: headless clients still cannot run skill workflows. |
| 111 | PY: `POST /agents/{id}/chat` streams bare `data: {json}\n\n` frames and a final `data: [DONE]\n\n` exactly as PHP (`AgentController.php:486,507`) via `SseStream.send_data`; every other stream keeps the named-event framing PHP uses there | ✅ | Byte-identical after Task 2 fix round 1. |
| 112 | PY: `OpenAIProvider::normalizeUsage` crashed on `"usage": {}` (PHP-array decode → `[]`, `.get` on a list) where PHP's `?? 0` on an empty array yields 0 | ✅ | Latent since Phase 2b; fixed in the Phase 5 final wave (list-shaped usage treated as empty). |
| 113 | PY: Playbook run tables (`playbook_runs`, `playbook_run_messages`, `playbook_run_notes`, `playbook_run_ledger`, `playbook_run_gates`) are assumed present (they exist live); PHP never creates them either | 🪞 | No DDL in either backend. |
| 114 | PY: `PlaybookAgentTools::forUser` wired into `AgentController::create/update` (closes the Phase 4 stub, row 99) | ✅ | Row 99 → resolved. |
| 115 | PY: `WorkflowRunner::executeTransformStep` snapshots `variables` explicitly (PHP array value semantics) — Python dict aliasing would otherwise make the transform output self-referential | ✅ | Caught by unit test. |
| 116 | PY: PHP `(array)$x` casts in the Playbook package (`fromArray` tools_used/actions_used, `runLeg` arguments, `redactSensitiveFields` fields, `_normalizeToolCalls`) go through `phpcompat.php_array_cast` (dict→dict, list→list, None→[], scalar→[scalar]) | ✅ | Phase 5 final wave. |
| 117 | PY: `phpjson.dumps_pretty` keeps slashes escaped by default — PHP call sites (`WorkflowOutputStorage.php:85`, `GraphWorkflowRunner.php:1600`) pass `JSON_PRETTY_PRINT \| JSON_UNESCAPED_UNICODE` only | ✅ | Pinned by a `php -r` comparison test. |
| 118 | PY: `GraphWorkflowRunner::getDocumentStorageAdapter` returns `null` (local fallback) even though live PHP's `file_exists(universalfs autoload)` guard passes — Python never talks to universalFS (Phase 3 ruling) | 🪞 | No document can be stored with `storage: 'remote'` through Python (`uploadNodeDocument` answers 503 like PHP without an adapter), so the branch is unreachable end-to-end. |
| 119 | PY: `GraphWorkflowRunner::extractPdfText` uses pypdf instead of `shell_exec('pdftotext')`; `basicPdfTextExtract` ported literally as the fallback | 🪞 | Text may differ slightly from poppler's output on complex layouts. |
| 120 | PY: `WorkflowController.runStream`/`runPlaybookNode`/`toolResult` each port PHP's own `if (!$userId)` "Authentication required" branch verbatim, even though the shared auth middleware already rejects unauthenticated requests before the controller runs, making all three branches dead code on live PHP (confirmed via unauthenticated `curl`, no `Authorization` header → plain JSON 401 from the middleware, not the controller) | 🪞 | `WorkflowController.php:894` (runStream), `:1036` (runPlaybookNode), `:1073-1076` (toolResult, plus its own `http_response_code(401)` call — also dead, same class as row 104). Ported anyway (mirror PHP's method body, not just its live-reachable subset); unit-tested, differential coverage compares the actual middleware-level 401. |
| 121 | PY: `GraphWorkflowRunner::_createExecution` encoded `inputVariables` with a bare `php_json_encode()`, no `php_array()` empty-collection wrap — an empty `{}` request body (this endpoint's own default) round-tripped as `"{}"` in `agent_workflow_executions.input_variables` vs PHP's `"[]"` for its empty array | ✅ | `GraphWorkflowRunner.php:1658-1671`. **Fixed in the Phase 5 final wave**: `graph_workflow_runner.py:_createExecution`/`_completeExecution` and `workflow_runner.py:_createExecution`/`_completeExecution` (same PHP-array-typed-param pattern in both files) now wrap with `php_array()` before encoding. Differential test's `[]`≡`{}` normalization removed (`tests/differential/test_workflow_run.py`); unit-pinned in `tests/unit/test_graph_workflow_runner_core.py` / `tests/unit/test_workflow_runner.py`. |
| 122 | PY: `phpcompat.str_word_count` is a regex-based approximation of PHP's locale-dependent C `str_word_count()` | 🪞 | `AgentDelegationFunctions.php:354`; diagnostic-only `result_word_count` field, not asserted by any oracle test. Moved from `AgentDelegationFunctions._str_word_count` into `phpcompat` in the Phase 5 final wave (C4) so other callers can share it. |
| 123 | PY: `GraphWorkflowRunner`'s Start-node "active glow" delay ported as `time.sleep(0.3)`, matching PHP's `usleep(300000)` | ✅ | `GraphWorkflowRunner.php:328`; adds ~300ms to every `run()` call on both backends, including in tests. |
| 124 | PY: `GraphWorkflowRunner::_ensureDbConnection` (and `AgentRunner::_ensureDbConnection`) is a lightweight `SELECT 1` probe + log, not a manual PDO-style rebuild — Python's `Db._run()` (Phase 2a) already retries once on a "gone away" error by reconnecting the same `Db` instance in place, and all repositories hold a reference to that same object, so PHP's repository-recreation-on-reconnect has no Python equivalent to port | 🪞 | `GraphWorkflowRunner.php:1708-1727` (reconnect + repo recreation), `AgentRunner.php:778`. Covered by `tests/unit/test_db_reconnect.py`. |
| 125 | PY: `ScheduledWorkflowService.calculateNextRun` reproduces PHP `DateTime::add(DateInterval)` arithmetic including month-end overflow (no clamping — e.g. `2026-01-31 + P1M` → `2026-03-03`) and Europe/Berlin DST spring-forward-gap re-localization | ✅ | `ScheduledWorkflowService.php` (`calculateNextRun`). 20-case fixture generated via live `php -r` probes, reproduced byte-for-byte by `tests/unit/test_scheduled_workflow_service.py::test_calculate_next_run_fixture_table`. |
| 126 | PY: `SchedulerController::run`'s `X-Scheduler-Token` header lookup is case-insensitive (Starlette) where PHP's `$request['headers']['X-Scheduler-Token']` reads the exact-case key | 🪞 | `SchedulerController.php:49`. Pre-existing header-handling convention; Python accepts an oddly-cased header PHP would treat as absent. |
| 127 | PHP+PY: `SchedulerController::run` with an unconfigured token (`$this->config['scheduler']['token'] ?? ''` → `''`) matches an explicitly sent empty token, via plain `!==`/`!=` (not `hash_equals`) | 🪞 | `SchedulerController.php:49-53`. Bug-for-bug; owner: require a configured token and switch to constant-time comparison. |
| 128 | PY: cron entry point `backend_python/scheduler/run_scheduled_workflows.py` ports only the CLI branch of `backend/scheduler/run-scheduled-workflows.php` — zero flag parsing (mirrors PHP's total absence of `getopt()`/`$argv` handling; `-h`/`--help` is silently ignored, same as any other arg) and byte-identical stdout line format (`[Y-m-d H:i:s] Found N due schedule(s)`, per-schedule ✓/✗ lines, `Completed: N succeeded, M failed`) | ✅ | HTTP branch (`php_sapi_name() !== 'cli'`, token-gated JSON) is superseded by `SchedulerController::run` (row 127), not re-ported here. Crontab line documented in the module docstring (`.venv/bin/python -m scheduler.run_scheduled_workflows`), not installed. |
| 129 | PY: cron script exit codes mirror PHP's CLI-branch codes only — `0` on normal completion (including zero due schedules), `1` on a config-load failure or DB-connect failure | ✅ | `backend/scheduler/run-scheduled-workflows.php:65` (missing config file), `:86` (`PDOException` on `new PDO(...)`) — the CLI-only `exit(1)` paths; lines 32/41/68/89 belong to the unported HTTP branch. Per-schedule DB-write order (`markRunning` → run → `markCompleted`/`markFailed`) matches PHP's own sequence. |
| 130 | PHP+PY: the cron script has no lock/overlap protection — no `flock()` or lock file in either backend | 🪞 | Confirmed by grepping the entire `backend/scheduler/` tree and its one-commit git history: no lock code ever existed. Overlap protection is left to `ScheduledWorkflowService.getDueSchedules()`'s `WHERE status = 'pending'` filter plus `markRunning()` flipping the row to `'running'` before work starts. Verified by `test_no_overlap_protection_two_runs_both_execute` (two back-to-back runs both complete). |
| 131 | PY: `register_mock_stack.py`'s find-after-upsert (`SELECT id FROM mcp_servers WHERE user_id=:u AND name=:n`, run immediately after the `ON DUPLICATE KEY UPDATE` upsert) has no PHP-style fallback for a missing row | 🪞 deliberate deviation | `register_mock_stack.php:87` — `(int)$findId->fetch()['id']` on a no-match `fetch()===false` emits a PHP warning ("Trying to access array offset on value of type bool") and continues with `$serverId = 0`; Python's `server_row['id']` would instead raise `TypeError`, propagating to `run()`'s catch-all → exit 255. Unreachable in practice: the immediately-preceding upsert guarantees the row exists. |
| 132 | PY: `firebase_auth_export._to_ms` parses `users.created_at`/`last_login` with a single exact `datetime.strptime('%Y-%m-%d %H:%M:%S')` where PHP's `$toMs` closure calls `strtotime((string)$s)` (accepts ISO-8601, relative strings, and many more formats) | 🪞 | `backend/scripts/firebase-auth-export.php:143`. Both fall back to "now" in ms on a parse failure; the narrowing is unreachable for the actual column values, which always arrive as MySQL `Y-m-d H:i:s` datetimes. |
| 133 | PHP+PY: `backend/scripts/test-ingestion-{compiler,loader,splitter}.php` and `test-vector-mcp-store.php` (four files) are plain-assert CLI test harnesses, not application code — not ported | 🪞 | Coverage moved to the Phase 6 unit suite (`test_ingestion_compiler.py`, `test_ingestion_loader.py`, `test_ingestion_splitter.py`, `test_vector_mcp_store.py`). |
| 134 | PHP: Apache `php.ini` has `serialize_precision=100` (CLI: -1), so PHP JSON leaks ~50-digit float noise on computed floats (`/admin/costs`, `/admin/usage/*`) | 🪞 | Environment quirk; the Phase 7 differentials round computed floats to 6 dp on both sides (`same_rounded`); Python emits the shortest repr. Owner: set `serialize_precision=-1` in Apache's php.ini. |
| 135 | PHP+PY: Hume tool routes are commented `// HUME TOOL ROUTES (Public)` in `routes.php` but are NOT in `MiddlewareProcessor::PUBLIC_ROUTES`; only `POST /api/v1/evi/webhook` is public | 🪞 | Comment is stale/misleading (confirmed via `grep -rn "hash_hmac\|X-Hume-Signature" backend/src/` and reading `PUBLIC_ROUTES` directly). `app/middleware/processor.py::PUBLIC_ROUTES` already carried the correct single entry; no change needed. |
| 136 | PHP+PY: `POST /api/v1/affiliate/conversions` is auth-required (not in `PUBLIC_ROUTES`), only not role-gated — `recordConversion()`'s own `if (!user_id) return 401` is belt-and-braces; bcrypt password hashing for affiliates lives in `AffiliateController::adminCreate`, NOT `LoginAdminController` (whose `createUser` never writes a password column — `provider=email`, `email_verified=0` only) | 🪞 | The Phase 7 brief's Sources section had both of these backwards; PHP wins. `AffiliateController::adminCreate` uses the existing `auth_controller.password_hash()` helper (bcrypt cost 10, `$2b$` — cross-verifies with PHP's `$2y$`, same precedent as row 19). |
| 137 | PHP+PY: `LoginAdminController::_requireAdmin` answers 403 for BOTH unauthenticated and non-admin callers; `AffiliateController::_requireAdmin`/`_requireAffiliate` answer 401 (unauthenticated) vs 403 (wrong role) | 🪞 | Per-controller PHP behavior mirrored as-is (each controller carries its own gate copy, not shared). |
| 138 | PY: `Services/HumeToolSyncService.php`, `AffiliateRepository.php`, `AffiliateCommission.php`, `AffiliateSupport.php` are ported as module-private classes inside their controllers' files (`_HumeToolSyncService` in `hume_tool_controller.py`; `_AffiliateRepository`/`_AffiliateCommission`/`_AffiliateSupport` in `affiliate_controller.py`) rather than under `app/services/` | ⏳ | Each is instantiated fresh by exactly one controller in PHP too (never shared elsewhere), so behavior is unaffected; deferred only because the tasks' file-staging guard scoped changes to the two controller files. TS already has standalone `Services/AffiliateRepository.ts`/`AffiliateCommission.ts`/`AffiliateSupport.ts` — final wave: extract PY's copies to `app/services/` to match. |
| 139 | PY: `PortfolioFunctions`/`portfolio_functions.py` DB-error message text differs from PHP's `PDOException::getMessage()` (`"SQLSTATE[42S02]: Base table or view not found: 1146 Table '...' doesn't exist"`) vs Python's bare `str(pymysql.err.Error)` (`"(1146, \"Table '...' doesn't exist\")"`) | ⏳ | Phase 2c gap, surfaced via Hume `get_portfolios` (Task 6); the differential case was switched to `get_asset_sentiment` to avoid the DB-missing-table landmine. Fix in a later final wave if the PHP text is deterministic enough to pin; otherwise mirror the shape only. |
| 140 | PHP+PY: `AdminController::toggleMCPServer`'s `UPDATE ... SET enabled=?, updated_at=NOW() WHERE id=?` uses the driver's default "affected rows" `rowCount()` (no `MYSQL_ATTR_FOUND_ROWS`/`client_flag` set) — re-toggling to the value a row is ALREADY at returns 0 affected rows (→ 404 "Server not found") only when two calls land within the same wall-clock second, since `updated_at=NOW()` then also produces an identical value | 🪞 | Live-probed against plain PHP alone (3 calls: 200/404/404) — genuinely non-deterministic in PHP itself, confirmed identical on PY. Not a PHP-vs-PY discrepancy; the round-trip test in `test_admin_usage_mcp_costs.py` sidesteps the window by only ever toggling to a *different* value each call. |
| 141 | PY: `AdminController.loadAllAvailableRegularTools` reads `claude_tools.json` from `../backend/resources/claude_tools.json` (the PHP repo tree) — `backend_python/resources/` has no copy, unlike `model_catalog.json`/`prompts` which earlier phases already mirrored there | ⏳ | Final-wave item: copy the file into `backend_python/resources/` for consistency; this round's scope was limited to `admin_controller.py`/`routes.py`/tests. |
| 142 | PY: `_filter_validate_url` (module-private URL-validation helper) is duplicated verbatim between `admin_controller.py` and `mcp_server_controller.py` rather than shared | ⏳ | Final-wave item: extract both copies into a shared helper module; `mcp_server_controller.py`'s existing comment ("no other ported controller validates URLs") is now stale. |
| 143 | PY: `VideoEditorController` — verified live against production PHP (HTTP + a direct PDO-vs-PyMySQL side-by-side query) that mysqlnd returns INT-typed columns (`COUNT()`, INT primary keys) as native PHP `int` regardless of `PDO::ATTR_EMULATE_PREPARES`; only DECIMAL columns and `SUM()`-over-numeric aggregates come back as strings — exactly what PyMySQL + `Db.normalize_value` already produce with zero special handling | ✅ | Corrected an initial (wrong) implementation that had stringified every non-null fetched value on the theory that PHP's default emulated-prepare mode stringifies everything; removed after live verification (`video_editor_controller.py`'s module docstring records both the wrong theory and the verified truth, so the trap isn't re-discovered). `tests/differential/test_admin_video_editor.py` 8/8 passed live, including the native-int fields. |
| 144 | PHP+PY: several PHP unit oracles are stale against current PHP and fail live — `PythonEmitHelpersPinTest::testSkillFsSyncBlockUnchanged` (6017≠5092), `WorkflowGraphAnalyzerAnalyzeTest` (unstubbed PDO), `AdkGeneratorEmitTest` + `adk_diamond.golden.py` (predate `_make_model(thinking=…)`), `MafGeneratorEmitTest` (predates the MAF rewrite), `backend/scripts/test-ingestion-loader.php` (calls a removed `IngestionLoader::decode()`); Python pins LIVE PHP behaviour (golden regenerated from `php -r`) | 🪞 | Owner: refresh the PHP fixtures. Earlier stale oracles: `GenesisProposerTest::buildWorkflowPrompt`, `AgentTest::testToApiArray`. |
| 145 | PY: PHP `\w` under the `/u` modifier is Unicode-aware; ASCII narrowing (`[A-Za-z0-9_]`) applies only to PHP patterns WITHOUT `/u` (all generator regexes use explicit ASCII classes and no `/u`) | ✅ | Ruling after a Phase 5 wave regression; verified with `php -r` on `#Résumé`. |
| 146 | PY: `PythonEmitHelpers::jsonToPython` float tokens follow PHP `json_encode` (`serialize_precision=-1`): `1.0`→`1`, `1.0E-5`→`1.0e-5`, `1e16`→`10000000000000000` | ✅ | 13 cases byte-identical to live PHP; `phpjson.dumps_pretty` gained an opt-in float hook, default unchanged. |
| 147 | PY: `LangGraphGenerator::loadMcpToolsWithServers` decodes MCP schemas NON-assoc (object-vs-array shape preserved on `{}`), and `wordwrap()` counts UTF-8 bytes — both mirrored (the em dash in the dispatcher prompt wraps identically) | ✅ | Caught by `php -r` byte-diff. |
| 148 | PY: `IngestionCompiler` reproduces PHP `(array)` casts on scalar `types`/`connection` (`"pdf"` → `["pdf"]`), and `IngestionLoader` uses PHP `basename()` trailing-slash semantics | ✅ | Shared `_ingestion_compat.php_array_cast`/`_php_basename` (final wave: promote to phpcompat). |
| 149 | PHP+PY: `IngestionController::saveScript`/`runStart` answer 404 for a missing id (no 400); `runWorker` reads `config['loader']['storage_mcp_id']`, which `runStart` never persists | 🪞 | Mirrored; owner may tidy. |
| 150 | PY: ingestion scripts written by `saveScript`/`runStart` are chmod 0o666 (dirs 0o777) so the PHP daemon can read them | ✅ | Phase 2d precedent. |
| 151 | PY: ingestion differential — `loader-text`/`splitter-chunks` skip when the langfs MCP server (id 46) is down; `DIFF_INGESTION=1` store-write round-trips are honest skips pending a collection-isolation strategy for the shared vector DB | ⏳ | Follow-up: isolated test collection, then real write parity. |
| 152 | PY: pure-literal PHP emitter methods (`dataFlowDoc`, `stateBlock`, … 13 in LangGraph) became module-level constants; output byte-identical | 🪞 | Structural only. |
| 153 | PY: `GraphWorkflowRunner`'s `buildAgentLLMRequest` is cross-checked, not byte-equal, against `ProviderRequestFactory` — the two PHP builders already diverge (Claude multi-turn vs single-turn) | 🪞 | Faithful to both PHP twins. |
| 154 | PY: `PythonEmitHelpers::jsonToPython` (forceObject=False) rendered an empty Python `dict` as `{}` — PHP's `json_encode([])` can't distinguish an empty assoc array from an empty list, so it always emits `[]`; `ADKGenerator.php:892-893`'s `NODE_NAMES` builder hit this for every workflow with zero agent-type nodes (playbook-only / ingestion-only: workflow ids 27, 38, 40, 41, 42) | ✅ | Fixed: empty dict + `forceObject=False` now emits `[]` (mirrors every dict-shaped, non-`forceObject` call site, not just `NODE_NAMES`). Pin test `test_json_to_python_empty_dict_without_force_object_renders_list`; verified against a live PHP `generate-adk` response for workflow 27. |
| 155 | PY: all-workflows generator differential — user 3's 24 workflows × 4 generators (LangGraph/ADK/MAF/NOOA) × 3 modes (default JSON, `?download=1`, `?a2a=1` for `generate-python` only) = 168 generation checks + not-found/unauthenticated, byte-identical after row 154's fix; the `Generated: ... by the SynergyAI workflow editor` docstring timestamp line is normalised to a placeholder using the identical regex PHP's own `GeneratedDocParityTest.php:102` uses (that PHP oracle doesn't byte-freeze it either) | 🧪 | `tests/differential/test_generators.py`. Workflow 14 has a genuine graph cycle/disconnected node — both backends return the identical `500 {"success": false, "error": "Workflow graph has a cycle or disconnected node"}` for all 4 generators. |
| 156 | PY: `test_generate_all_workflows_download_mode_parity` intermittently hit `httpx.ReadTimeout` (fixed 60s client timeout) against the shared live PHP + Python servers under concurrent Phase 6/7/8 agent load — an isolated timing probe of the same 96 requests run alone found zero over 5s, confirming PHP itself is fast; contention from other agents' concurrent requests, not a generator bug | 🪞 | 2-retry-with-backoff wrapper added around the test's loop only, retrying on `ReadTimeout` alone — every retry still compares the eventual response byte-for-byte identically, so it's correctness-neutral. |
| 157 | PY: CORS — when a request carries an `Origin` header the Python backend echoes it (`Access-Control-Allow-Origin: <origin>`) and adds `Access-Control-Allow-Credentials: true` + `Vary: Origin`; PHP hardcodes `*` (`CorsMiddleware.php:54`). Browsers reject `*` for the frontend's `credentials: 'include'` fetches, which silently blocked every workflow-node `POST /chat` when the frontend ran cross-origin against `localhost:3002` (preflight logged, POST never sent). Requests without `Origin` keep PHP's exact headers. `CORS_ALLOWED_ORIGINS` (comma list) restricts echoed origins; unset = any. | ✅ | PHP is same-origin so never exercised; apply the same change in PHP if it is ever hosted cross-origin. |
| 158 | **Swarm orchestration (2026-09-16).** PHP gained a `orchestration` column on `agent_workflows` (`workflow`\|`swarm`, default `workflow`) and everything that carries it: `Workflow` model field + `hydrate` + both array builders + getter/setter; `WorkflowController::create` (setter chain) and `::update`; **`WorkflowRepository`'s five explicit column sites** (INSERT columns, INSERT values, INSERT params, UPDATE set-list, UPDATE params); migration `backend/migrations/add_workflow_orchestration.sql` (applied). Plus `run()` and `run-stream` now **refuse** a swarm workflow — both walk the graph topologically, so a swarm would have executed as a DAG with Start's fan-out running concurrently. Neither twin has `orchestration` anywhere in its workflow model/controller/repository (TS's two hits are `MAFGenerator`/`GraphWorkflowRunner`, unrelated). | ✅ | ⬜ | ⬜ | — | PHP verified live by the owner 2026-09-16 (conversational overlay works end to end). **The repository column list is the trap**: the model and API can carry the field correctly while it never reaches the database, and no unit test sees it — that exact failure happened in PHP first. Port with a save/read round-trip, not just unit tests. `SwarmRewriter.php` + its fixture are NOT needed in the twins yet — the rewrite runs in the editor (JS) for v1(a); the PHP copy is groundwork for the compiler (v1b), and a twin is only owed once a twin compiles. |

## How items get verified
Each fix is validated by **differential testing against the live PHP backend** (byte/semantic
compare) before its 🧪 box is checked. Changes are left unstaged for the repo owner to commit;
schema changes ship as `.sql` to run manually.
